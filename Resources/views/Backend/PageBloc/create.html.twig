{% extends "WHSmartAdminBundle::popup.html.twig" %}

{% form_theme form 'bootstrap_3_layout.html.twig' %}

{% block title %} Créer {% endblock %}

{% block body %}

{{ form_start(form, {'action': path('wh_admin_cms_bloc_create', {'template' : template.id}), 'method': 'POST'}) }}

    <div class="padded">

        {% include('WHCmsBundle:Backend:Bloc/form.html.twig') %}
        {{ form_row(form.Parent) }}
        {{ form_row(form.Template) }}

    </div>

    {{ Smart.modalSubmitBtn() }}

{{ form_end(form) }}

{{ Smart.modalSubmitAjax() }}

{% endblock %}
